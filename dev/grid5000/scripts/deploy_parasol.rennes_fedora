#! /bin/bash

source $(dirname $0)/g5k_lib.sh

CLUSTER=parasol.rennes
if [ $# -gt 0 ] ; then
	NODES=$*
else 
	NODES=$(`dirname $0`/g5k_reservedNodes -s $CLUSTER)
fi
KADEPLOY_ARGS="-e fedora4all -p sda3 -l deploy"
MOUNT_ARGS="nfs-grid5000:/srv/home /home"

. $(dirname $0)/deploy/deploy
