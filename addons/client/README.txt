You can put in this addons folder jars or classes folders to be included in the
Node forked JVM's classpath.
These libraries will only be loaded when a task is executed on a Node and only when the default fork mode is used.

