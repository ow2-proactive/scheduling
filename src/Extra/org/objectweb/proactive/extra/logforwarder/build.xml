<?xml version="1.0" encoding="UTF-8"?>
<project name="A Log Forwarder" basedir="../../../../../../../compile/">

	<import file="${basedir}/build.xml" />
	<target name="compile" depends="core,-compile"/>

	<target name="-compile">
		<if>
			<bool>
				<contains string="${java.version}" substring="1.5"/>
			</bool>
			<echo>Log Forwarder requires Java 6 or newer ; Skipping Log Forwarder compilation...</echo>
			<else>
				<compile_extra module="logforwarder" />
			</else>
		</if>
	</target>
</project>
