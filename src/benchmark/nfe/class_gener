#!/bin/sh

compteur=0
limite=`expr $1 - 1`
while [ $compteur -le $limite ]; 
do
filename=$(echo "handler"$compteur".java")
echo "" > $filename
echo "package benchmark.nfe;" >> $filename
echo "import org.objectweb.proactive.core.exceptions.handler.HandlerCommunicationException;" >> $filename
echo "public class handler"$compteur" extends HandlerCommunicationException {" >> $filename
echo "}" >> $filename
compteur=`expr $compteur + 1` 
done;