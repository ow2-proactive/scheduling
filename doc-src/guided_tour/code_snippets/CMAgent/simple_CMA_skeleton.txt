//Main Class

public class Main {
    public static void main(String args[]) {
	// 1. create the Monitoring agent using 
	//   org.objectweb.proactive.api.PAActiveObject.newActive(...)
	// 2. call the getCurrentState() and display the results
	// 3. Stop the active object using  PAActiveObject.terminateActiveObject(...)
        }
    }
}

