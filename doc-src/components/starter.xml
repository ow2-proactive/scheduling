<chapter xmlns="http://docbook.org/ns/docbook" version="5.0"
	xml:id="starter">
	<info>
		<title>Getting started</title>
	</info>
	<para />
	<para>
		For starters, here is a very basic example demonstrating the
		separation between the code and the deployment of an application
		and also showing the simplicity with which the deployment can be
		modified.
	</para>

	<para>
		As shown in the diagram below, in the first step, The
		application is just composed of two primitive components
		distributed into a single Java Virtual Machine.
	</para>

	<mediaobject>
		<imageobject>
			<imagedata scalefit="1" width="100%" contentdepth="100%"
				fileref="components/pics/distributed_components.png" format="PNG" />
		</imageobject>
	</mediaobject>

	<para>
		Now, in order to use two separate Java Virtual Machine, in the
		deployment descriptor file, the line:
	</para>
	<para>
		<programlisting xml:lang="xml">
			&lt;host id="localhost" os="unix" hostCapacity="1"
			vmCapacity="2"&gt;
		</programlisting>
	</para>
	<para>is changed to:</para>
	<para>
		<programlisting xml:lang="xml">
			&lt;host id="localhost" os="unix" hostCapacity="2"
			vmCapacity="1"&gt;
		</programlisting>
	</para>

	<para>
		Before changing the line, the deployment descriptor indicates
		that there will have 1 Java Virtual Machine with 2 nodes inside the
		JVM.
	</para>
	<para>
		Then, once the changed made, the deployment descriptor specifies
		that it must have 2 Java Virtual Machines with 1 node per JVM:
	</para>

	<mediaobject>
		<imageobject>
			<imagedata scalefit="1" width="100%" contentdepth="100%"
				fileref="components/pics/2components_2jvms.png" format="PNG" />
		</imageobject>
	</mediaobject>

	<para>
		All the source files are available at the end of the part in
		<xref linkend="annex" />
		.
	</para>
	.
</chapter>

