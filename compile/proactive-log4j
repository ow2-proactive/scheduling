# This file represents the log4j config file for non regression tests.
# It is given as property (in the proactive.xml) when starting nonregression tests

# The default logging level is INFO
# The root logger logs in the test.log file
log4j.rootLogger= INFO,FILE

#Both tests packages log on the console
log4j.logger.nonregressiontest=INFO, CONSOLE
#log4j.logger.nonregressiontest=DEBUG, CONSOLE
log4j.logger.testsuite=INFO, CONSOLE

########### The following are existing categories in ProActive
#log4j.logger.proactive = INFO, CONSOLE
#log4j.logger.proactive.classloader = DEBUG, CONSOLE
#log4j.logger.proactive.events = DEBUG, CONSOLE
#log4j.logger.proactive.runtime = DEBUG, CONSOLE
#log4j.logger.proactive.body = DEBUG, CONSOLE
#log4j.logger.proactive.mop = DEBUG, CONSOLE
#log4j.logger.proactive.groups = DEBUG, CONSOLE
#log4j.logger.proactive.sync_call = DEBUG, CONSOLE
#log4j.logger.proactive.deployment = DEBUG, CONSOLE
#log4j.logger.proactive.deployment.log = DEBUG, CONSOLE
#log4j.logger.proactive.deployment.process = DEBUG, CONSOLE
#log4j.logger.proactive.deployment.filetransfer = DEBUG,CONSOLE
#log4j.logger.proactive.filetransfer = DEBUG, CONSOLE
#log4j.logger.proactive.nfe = FATAL
#log4j.logger.proactive.gc = DEBUG, CONSOLE
#log4j.logger.proactive.ft = DEBUG, CONSOLE
#log4j.logger.proactive.ft.cic = DEBUG, CONSOLE
#log4j.logger.proactive.ft.pml = DEBUG, CONSOLE
#log4j.logger.proactive.communication.transport.http = DEBUG, CONSOLE
#log4j.logger.proactive.communication.rmi = DEBUG, CONSOLE
#log4j.logger.proactive.communication.ssh = DEBUG, CONSOLE
#log4j.logger.proactive.communication.transport.http = DEBUG, CONSOLE
#log4j.logger.proactive.migration = DEBUG, CONSOLE
#log4j.logger.proactive.communication.requests = DEBUG, CONSOLE
#log4j.logger.proactive.examples = DEBUG, CONSOLE

#log4j.logger.proactive.components = DEBUG, CONSOLE
#log4j.logger.proactive.components.requests = DEBUG, CONSOLE
#log4j.logger.proactive.components.activity = DEBUG, CONSOLE
#log4j.logger.proactive.components.bytecodegeneration = DEBUG, CONSOLE
#log4j.logger.proactive.components.adl = DEBUG, CONSOLE
#log4j.logger.proactive.components.gui = DEBUG, CONSOLE

#log4j.logger.proactive.p2p = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.startservice = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.vn = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.vn.service = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.nodes = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.acquaintances = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.first_contact = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.skeletons = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.skeletons.worker = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.skeletons.manager = DEBUG, CONSOLE
#log4j.logger.proactive.p2p.daemon = DEBUG, CONSOLE

#log4j.logger.proactive.security = DEBUG, CONSOLE
#log4j.logger.proactive.security.node = DEBUG, CONSOLE
#log4j.logger.proactive.security.session = DEBUG, CONSOLE
#log4j.logger.proactive.security.body = DEBUG, CONSOLE
#log4j.logger.proactive.security.manager = DEBUG, CONSOLE
#log4j.logger.proactive.security.request = DEBUG, CONSOLE
#log4j.logger.proactive.security.runtime = DEBUG, CONSOLE
#log4j.logger.proactive.security.policy = DEBUG, CONSOLE
#log4j.logger.proactive.security.policyserver = DEBUG, CONSOLE
#log4j.logger.proactive.security.crypto = DEBUG, CONSOLE
#log4j.logger.proactive.security.psm = DEBUG, CONSOLE

#log4j.logger.proactive.skeletons = DEBUG, CONSOLE
#log4j.logger.proactive.skeletons.kernel = DEBUG, CONSOLE
#log4j.logger.proactive.skeletons.structure = DEBUG, CONSOLE
#log4j.logger.proactive.skeletons.manager = DEBUG, CONSOLE
#log4j.logger.proactive.skeletons.application = DEBUG, CONSOLE

#log4j.logger.proactive.scheduler = DEBUG, CONSOLE
#log4j.logger.proactive.taskscheduler = DEBUG, CONSOLE
#log4j.logger.proactive.scheduler.commandlistener = DEBUG, CONSOLE
#log4j.logger.proactive.scheduler.jobTemplate = DEBUG, CONSOLE
#log4j.logger.proactive.scheduler.ressourceManager = DEBUG, CONSOLE
#log4j.logger.proactive.scheduler.ressourceListener = DEBUG, CONSOLE
#log4j.logger.proactive.scheduler.jobManager = DEBUG, CONSOLE

# Appender FILE writes to the file "tests.log".
log4j.appender.FILE=org.apache.log4j.FileAppender
log4j.appender.FILE.File=tests.log
# the following recreates a file for each run
log4j.appender.FILE.Append=false
# Appender FILE uses the PatternLayout.
log4j.appender.FILE.layout=org.apache.log4j.PatternLayout
# %X{hostname} adds the hostname
# %X{runtime} adds the runtime url
log4j.appender.FILE.layout.ConversionPattern=%5p [%t]: %m%n


#CONSOLE uses PatternLayout
#and displays the associated message (%m)
#using the platform dependant separator (%n)
#Use %M for method names, 
#%X{hostname} for hostname and %X{runtime} for runtime url
#see log4j documentation for details

log4j.appender.CONSOLE=org.apache.log4j.ConsoleAppender
log4j.appender.CONSOLE.layout=org.apache.log4j.PatternLayout
# %X{hostname} adds the hostname
# %X{runtime} adds the runtime url
#log4j.appender.CONSOLE.layout.ConversionPattern=%X{hostname} - [%20.20c{2}] %m %n
log4j.appender.CONSOLE.layout.ConversionPattern=%m %n
