#!/bin/bash
#build file for Unix system
compileDir=$(cd `dirname $0` && /bin/pwd)
if [ $# -lt 1 ]
then
  args="-projecthelp"
else
  args=$@
fi
# Go inside the ProActive directory
cd "$compileDir/.."
ant -buildfile  "$compileDir"/build.xml $args -lib  "$compileDir"/lib/AntelopeTasks_3.2.19.jar
