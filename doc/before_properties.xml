<?xml version="1.0" encoding="UTF-8"?>
<project name="doc_before_properties">
    <property name="doc.name"	value="Scheduler" />

     <!-- ##used for DocBookize## -->
    <!-- the path for java file inclusion -->
   <property name="java.inclusion" value="${src.scheduler.dir}/${scheduler.path}/" />
    
  <property name="xml.inclusion" value="${doc.dir}/" />

    <!-- The path to find java files in html -->
   <property name="otherpath1.inclusion" value="Scheduler_src_html/${scheduler.path}/" /> <!-- broken -->

   <property name="otherpath2.inclusion" value="${src.examples.dir}/${scheduler.path}/" />
    <property name="otherpath3.inclusion" value="./"/>
    <property name="otherpath4.inclusion" value="./"/>
    <property name="otherpath5.inclusion" value="./"/>
    
</project>    