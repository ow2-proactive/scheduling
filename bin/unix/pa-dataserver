#!/usr/bin/env bash

CLASSPATH=.
workingDir=`dirname $0`
. $workingDir/env log4j-client

eval $JAVACMD org.objectweb.proactive.extensions.vfsprovider.console.PADataserverStarter $@

retCode=$?
echo
exit $retCode
