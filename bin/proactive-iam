#!/usr/bin/env bash

ready="\(.*INFO.*DefaultCasEventListener.*Ready to process requests\)"

echo "Starting IAM microservice"

while read -r line
do
    echo -n "."
    if echo "$line" | grep -q "$ready"; then
       echo -e "\nIAM microservice started";
       break
    fi

done < <($1)
